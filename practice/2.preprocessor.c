#include <stdio.h> // always in lowercase
/*
    anything start with pound symbol (hash) without space
                        is known as preprocessor directive
*/
/*
    stdio.h(standard input,output.header) is the standard c library header which provides
    functionality for displaying output(printf) , among many other things
*/
/* in <stdio.h> and "stdio.h"
    angular brackets (< >) : look for a file in standard file directories (come with c )
    double quotes (" ") :  look for file in current dir (user defined header file )

 */
int main()
{
    printf("this is chaitanya chandra \n"); // printf is a standard library function
    return 0;
}