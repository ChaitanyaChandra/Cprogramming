/*  ram (random access memory) have ordered sequence of boxes
    the box is full represents 1
    when it is empty it represents 0
    each box represents one binary digit , either 0 or 1 (false or true)
    each box is called bit
 */
/*
  byte : bits in memory grouped into sets of eight(byte)
  each byte have uniq address
 */
#include<stdio.h>
int main()
{
    int a = 40;
    printf("\n%d",a); //40
    a = 50;
    printf("\n%d",a); //50
}